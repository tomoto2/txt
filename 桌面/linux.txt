需要对配置中的mysql和redis地址进行修改，修改为实际地址

初次运行先将com.joe.Application中的初始化redis脚本放开，在redis中生成房间号，生成完毕后关闭，把初始化脚本注释然后重新启动即可

//运行时直接用mvn package命令打包为jar包，然后用java -jar server.jar运行即可
直接切换到源码根目录   然后使用mvn spring-boot:run命令运行
（
nohup.out   文件记录启动日志
run.sh:
mvn clean
nohup mvn spring-boot:run&
）

linux清空redis，redis-cli   ，  flushall 
1.  AbstractRoom -=---测试时可以注释下面这个判断

2.首次启动项目运行时候，需要显示，后期运行时候需要注释掉，--------application 初始化redis脚本

阿里云服务器，域名，使用https进行访问，申请---审核---安装证书---按照自己服务器的配置选择安装方式----tomcat方式安装证书---在conf/server.xml中进行一下配置。
https的默认请求端口号是443。配置完成之后，在浏览器https://配置域名:443/,,,443可以不写，因为是默认端口。
	 <Connector port="443" 
               protocol="HTTP/1.1"
               maxThreads="150" 
               SSLEnabled="true" 
               scheme="https" 
               secure="true"
               clientAuth="false" 
               SSLProtocol="TLS"             
               keystoreFile="cert/214229725160084.pfx"
               keystoreType="PKCS12"
               keystorePass="214229725160084"
                />
				
				
				<xml><return_code><![CDATA[FAIL]]></return_code>
<return_msg><![CDATA[appid and openid not match]]></return_msg>
</xml>


安装jdk，
安装mysql，
tomcat，
redis,



/home/project/build-qmkc.sh
  XXXX.sh
#!/bin/sh
cd /home/project/qmkc
svn update
mvn clean
mvn package
mv /home/project/qmkc/target/qmkc-1.0.war /home/app/tomcat7/webapps/qmkc.war
rm -rf /home/app/tomcat7/webapps/ROOT
rm -rf /home/app/tomcat7/webapps/qmkc

tail logs/catalina.out   查看tomcat启动输出信息
sh /bin/restart.sh    重启服务器

, fetch = FetchType.LAZY //查询父类


以下是登陆密码：
139.129.98.158/ Shanhui123/
winscp：华北服务器：121.42.56.156  端口：9900 用户：root 密码：4O9net.com     pengxin,,, lipengxin
服务器（登录不了）：47.90.57.80  9900 root  $O9net.c0m
centos系统的密码都是$Onet.c0m
服务器：windows系统的  47.89.50.22  administrator $O9net.c0m

数据库：
121.42.56.156  root 空  
47.90.57.80:9999    root    4O9net.com
121.42.56.156：9999 axj @1x1ngju@n 
139.129.98.158/ root数据库密码：shanhui123

druid.datasource.url = jdbc:mysql://47.90.57.80:9999/qmkc?characterEncoding=utf8
druid.datasource.username = root
druid.datasource.password = 4O9net.com

域名地址：
http://www.hfcjjw.com/


重启服务器：
cd /home/app/tomcat7
sh bin/shutdown.sh
sh /home/project/build-qmkc.sh
sh /home/app/tomcat7/bin/startup.sh

1、先切换到：cd tomcat/logs
2、tail -f catalina.out
3、这样运行时就可以实时查看运行日志了
Ctrl+c 是退出tail命令。
控制台动态输出方式启动 ./catalina.sh run        直接启动 ./startup.sh      关闭tomcat服务./shutdown.sh
ps -ef |grep tomcat
ps -ef |grep java---------douniu
kill -9 2005
查看端口状态：netstat -ntlp

nginx
 ps -ef|grep nginx
cd /usr/local/nginx/sbin
./nginx -s reload 重启服务
./nginx 启动服务


svn重启
svnserve -d -r /home/svndata 
华北服务器，启动svn
svnserve -d -r /home/app/svn
查看svn线程状态
ps -ef|grep svnserve
杀死进程（id=100）
kill -s 9 100
修改服务器密码
passwd

搜索
在linux下如果要查找包含某个关键字的文件，如要在/root/目录下搜索带有关键字“www”的文件，在终端下运行命令：
find /root/ –type f |xargs grep “www”
grep -r "test" /root/
grep -r "www" /root/ 更简单


重启redis
用systemctl  restart  redis命令
重启数据库


控制台打出war包
到D:\work\back这个路径后，jar -cvf back.war *
使用eclipse
直接右键到处war

删除文件夹实例：
rm -rf /var/log/httpd/access
将会删除/var/log/httpd/access目录以及其下所有文件、文件夹
2
删除文件使用实例：
rm -f /var/log/httpd/access.log
将会强制删除/var/log/httpd/access.log这个文件




svn 报错解决方案
 Couldn't perform atomic initialization
svn: Couldn't perform atomic initialization
Internal error
svn: Can't find a temporary directory: Internal error

服务器磁盘满了。
查看磁盘使用情况
df -h
删除一些日志文件。
就ok了，可以进行提交更新操作了。





nginx
111111111111111111111111111
  server {
        listen       80;
        server_name  www.409net.com;
	index  index.html;	
        root   /409;

        #charset koi8-r;

        #access_log  logs/host.access.log  main;

     location  / {
            root   /409;
            index  index.html;
	    proxy_pass  http://www.409net.com/409;
	    client_max_body_size 10m;
        }
    


    22222222222222222222222222222222
        listen       80;
        server_name  localhost;
        #charset koi8-r;
        #access_log  logs/host.access.log  main;

        location / {
	      #rt /409/;
	      #index  index.html;
             #proxy_pass http://127.0.0.1:8080;
	     proxy_pass  http://127.0.0.1:8080;
	     proxy_set_header  X-Real-IP $remote_addr;
	     client_max_body_size 10m;
        }




333333333333333333333333333

    server {
        listen       80;
        server_name  www.sanhui.shop;
	root   /marketplace;
        index  login.html;	

        #charset koi8-r;

        #access_log  logs/host.access.log  main;

        location  / {
            root   /marketplace;
            index  login.html;
	    proxy_pass  http://www.sanhui.shop:8080/marketplace;
	    client_max_body_size 10m;
        }
   
   
   
   ys的无线密码:youshengtec001








